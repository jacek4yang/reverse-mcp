# Build
/target
Cargo.lock.bak

# Proprietary IDA material — never commit
*.7z
*.i64
*.idb
*.hexlic
ida.dll*
ida32.dll
idalib.dll
idalib32.dll
hex*.dll
idacli
IDA_Professional/
ida-pro-*/

# Task files
GROK_TASK.md

# Runtime artifacts (portable layout, exe-relative)
/cache/
/sessions/
*.log

# Vendored idalib SDK material - proprietary, never commit
vendor/idalib-sys/sdk/
vendor/idalib94-sys/sdk/

# ABI probe build artifacts
backends/abi/abi_probe
backends/abi/abi_probe.exe
backends/abi/abi_probe.o
backends/abi/abi_dump
backends/abi/abi_dump.exe
backends/abi/abi_dump94.exe
backends/abi/expected_facts.inc

# Local build/test logs
*.log

# Fixture build artifacts
tests/fixtures/simple.exe
tests/fixtures/ctf_*.exe
tests/fixtures/ctf_rust.pdb
tests/fixtures/*.ilk
tests/fixtures/*.obj
tests/fixtures/*.pdb

# Secrets and credentials — never commit any key material
*.key
*.pem
*.authkey
secrets/
.reverse-mcp-secrets/
# researcher-local provisioning helper (references the secret's location)
scripts/mb_fetch.py

# Tier-B local malware corpus (issue #64) — samples, manifests, dossiers
# and reports are researcher-local only; never enter Git.
corpus_local/
docs/malware_acceptance/dossiers/
docs/malware_acceptance/report.json
docs/malware_acceptance/manifest.local.json
tests/fixtures/largefn/*.obj
tests/fixtures/largefn/*.lib
